// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['tech.jayacm.co.id'], // Add the domain of your image source
  },
};

module.exports = nextConfig;
